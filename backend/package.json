{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "build": "cd ../frontend && npm install && npm run build && cd ../backend && node moveBuild.js",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.25.2",
    "@google-cloud/text-to-speech": "^5.3.0",
    "@huggingface/inference": "^2.8.0",
    "axios": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "langchain": "^0.2.16",
    "mongoose": "^7.0.3",
    "node-fetch": "^3.2.0"
  }
}
